{
  "name": "nostr-keyx",
  "version": "1.2.0",
  "description": "A NIP-07 browser extension that uses the OS's native keychain application to protect your private keys.",
  "private": true,
  "type": "module",
  "files": ["dist"],
  "scripts": {
    "build-js": "esbuild --bundle --minify --format=esm --outdir=dist src/*",
    "build-node": "esbuild --bundle --minify --format=esm --platform=node --outdir=dist --out-extension:.js=.mjs node/*.ts",
    "build": "rimraf dist ; ncp public dist ; concurrently \"npm:build-*\"",
    "watch-js": "esbuild --watch --bundle --sourcemap=inline --format=esm --outdir=dist src/*",
    "watch-node": "esbuild --watch --bundle --sourcemap=inline --format=esm --platform=node --outdir=dist --out-extension:.js=.mjs node/*.ts",
    "watch": "concurrently \"npm:watch-*\"",
    "pack": "rimraf *.tgz *.zip ; npm pack ; tar tfz *.tgz | sed -e 's|^package/||' | zip -r@ `ls *.tgz | sed -e 's|\\.tgz|\\.zip|'`"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/susumuota/nostr-keyx.git"
  },
  "keywords": [
    "nostr",
    "NIP-07",
    "chrome-extension",
    "browser-extension",
    "encryption",
    "keychain"
  ],
  "author": "Susumu OTA",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/susumuota/nostr-keyx/issues"
  },
  "homepage": "https://github.com/susumuota/nostr-keyx#readme",
  "devDependencies": {
    "@tsconfig/node-lts-strictest-esm": "^18.12.1",
    "@types/chrome": "^0.0.217",
    "@types/react": "^18.0.28",
    "@types/react-dom": "^18.0.10",
    "concurrently": "^7.6.0",
    "esbuild": "^0.17.10",
    "ncp": "^2.0.0",
    "rimraf": "^4.1.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@emotion/react": "^11.10.5",
    "@emotion/styled": "^11.10.5",
    "@mui/material": "^5.11.10",
    "@noble/secp256k1": "^1.7.1",
    "@scure/base": "^1.1.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
